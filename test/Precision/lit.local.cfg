# The precision harness is a developer tool, not a lit test: it sweeps tens of
# thousands of bit patterns per op and takes far too long for the regression
# suite. The cases it has already caught are pinned by the .mlir tests in
# ../functional/. Keep lit out of this directory entirely.
config.suffixes = []
